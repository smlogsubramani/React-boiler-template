import styles from "../../styles/components/forms/login-form.module.css";

const LoginForm = () => {
    return (
        <div>
            <h1 className={styles.heading}>This is Login form.</h1>
        </div>
    );
}

export default LoginForm;
